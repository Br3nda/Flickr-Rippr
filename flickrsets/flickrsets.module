<?php
/**
 * @file reads flickr set from flickr.com, 
 * From this you can copy markup, or save a a node
 * Does not stay in sync with flickr... at least not yet.
 */
 
/** 
 * Hook_perm
 */
function flickrsets_perm() {
  return array('Read Flickr Sets');
}
function flickrsets_menu() {
  $items = array();
  $items['flickr/sets'] = array('title' => t('Flickr Sets'),
                                 'page callback' => 'flickrsets_page',
                                 'access arguments' => array('Read Flickr Sets'));
                                     
  return $items;
}
                                 
function flickrsets_page() {
  $output = '';
  
  $output .= drupal_get_form('flickrsets_form');
  return $output;
}

function flickrsets_form() {
  $form =array();
  $form['flickrusername'] = array(
                                  '#type' => 'textfield', 
                                  '#title' => t('Flickr user name'),
                                  '#required' => true);
  $form['submit'] = array(
                          '#type' => 'submit',
                          '#value' => t('Get Sets'),
                         );
  return $form;
}

function flickrsets_form_submit($form, $values) {
  $flickr = flickrrippr_phpFlickr();
  $flickr->
  //put into temp table
  //redirect and display
}